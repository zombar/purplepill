{{- if .Values.scraper.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "docutag.fullname" . }}-scraper
  labels:
    {{- include "docutag.componentLabels" (dict "component" "scraper" "type" "backend" "context" .) | nindent 4 }}
spec:
  type: {{ .Values.scraper.service.type }}
  ports:
  - port: {{ .Values.scraper.service.port }}
    targetPort: http
    protocol: TCP
    name: http
  selector:
    {{- include "docutag.componentSelectorLabels" (dict "component" "scraper" "context" .) | nindent 4 }}
{{- end }}
