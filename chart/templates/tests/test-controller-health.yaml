{{- if .Values.controller.enabled }}
apiVersion: v1
kind: Pod
metadata:
  name: {{ include "docutag.fullname" . }}-test-controller
  labels:
    {{- include "docutag.labels" . | nindent 4 }}
    app.kubernetes.io/component: test
  annotations:
    "helm.sh/hook": test
    "helm.sh/hook-delete-policy": before-hook-creation,hook-succeeded
spec:
  restartPolicy: Never
  containers:
  - name: test
    image: curlimages/curl:latest
    command:
      - sh
      - -c
      - |
        set -e
        echo "Testing controller health endpoint..."

        # Wait for service to be ready
        sleep 5

        # Test health endpoint
        response=$(curl -s -o /dev/null -w "%{http_code}" http://{{ include "docutag.fullname" . }}-controller:8080/health)

        if [ "$response" = "200" ]; then
          echo "✓ Controller health check passed (HTTP $response)"
          exit 0
        else
          echo "✗ Controller health check failed (HTTP $response)"
          exit 1
        fi
{{- end }}
